#include "holberton.h"
/**
 * add - adds two integers and returns the result
 * @a: an integer to add to b
 * @b: an integer to add to a
 * Return: Void.
 */

int add(int a, int b)
{
	return (a + b);
}
